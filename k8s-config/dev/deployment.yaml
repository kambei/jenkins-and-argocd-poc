apiVersion: apps/v1
kind: Deployment
metadata:
  name: poc-app-deployment
  namespace: dev
  labels:
    app: poc-app
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: poc-app
  template:
    metadata:
      labels:
        app: poc-app
        environment: dev
    spec:
      containers:
      - name: poc-app-container
        # Image name updated to local-only reference
        image: poc-web-app:v0.0.0-PLACEHOLDER
        imagePullPolicy: IfNotPresent  # Use local Docker cache if available
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: poc-app-service
  namespace: dev
spec:
  selector:
    app: poc-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer

